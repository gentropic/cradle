{
  "name": "cradle",
  "version": "0.1.0",
  "private": true,
  "description": "Static capsule bootloader + curated renderers (menu, doorbell, arcr).",
  "scripts": {
    "build": "node build/build.js",
    "test": "node --test --test-concurrency=1"
  },
  "license": "MIT"
}
